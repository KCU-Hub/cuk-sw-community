// Supabase Storage bucket identifiers. Lives in a dedicated file (no
// server-only imports) so client components can import without dragging
// the server Supabase client into their bundle.

export const COURSE_FILES_BUCKET = "course-files";
